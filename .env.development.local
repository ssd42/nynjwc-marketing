# Local dev only — Vite loads this in `npm run dev`, never in a prod build.
# The `.local` suffix keeps it out of git. Points the submit page (and the
# landing waitlist form) at the local backend.
VITE_API_BASE_URL=http://localhost:8000
